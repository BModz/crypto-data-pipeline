name: crypto_pipeline
version: "1.0.0"
profile: crypto_pipeline

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  crypto_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
